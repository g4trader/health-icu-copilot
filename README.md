# Health ICU Copilot (Mock)

Protótipo inicial de um agent de IA para UTI (Hospital Moinhos de Vento),
com dados mockados e interface pronta para deploy na Vercel.

## Scripts

- `npm install`
- `npm run dev`

## Conceito

- Coluna esquerda: lista de pacientes priorizados por risco.
- Coluna direita: chat com o agente, consumindo APIs mockadas em `/api`.
